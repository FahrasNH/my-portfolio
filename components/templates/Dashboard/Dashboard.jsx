import React from 'react'
import {
	Header,
	CodeSlider,
} from '../../organisme'

const Dashboard = ({ router }) => (
	<>
		<Header router={router} />
		<CodeSlider />
	</>
)

export default  Dashboard
